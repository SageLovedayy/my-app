import Layout from "@/components/layout";
import { Input } from "@mui/material";

export default function HealthProfile() {
  return (
    <Layout>
      <div className="relative">
        <p>This is the health profile page</p>
      </div>
    </Layout>
  );
}
