import Layout from "@/components/layout";
import { Input } from "@mui/material";

export default function TrainingPrograms() {
  return (
    <Layout>
      <div className="relative">
        <p>This is the training programs page</p>
      </div>
    </Layout>
  );
}
